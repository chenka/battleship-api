# battleship-api
For interview

## Prerequisites
Node.js

MongoDB

## Installing
```
npm install
npm start
```

## API
`POST /attack/:gameId`

`POST /deploy/:gameId`

`POST /games`

`POST /attack/:gameId`

`GET /grids/:gameID`

## Running the tests
`npm test`
